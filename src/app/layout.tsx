import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "First-Principles Product Incubator | Innovation Workspace",
  description: "An innovation workspace that guides product, engineering, and design teams from ideation to validated MVP. Leverage MIT First Principles, Design Thinking, Theory of Constraints, and Lean Startup methodology.",
  keywords: ["product incubator", "first principles", "design thinking", "theory of constraints", "lean startup", "innovation", "MVP"],
  authors: [{ name: "Product Incubator Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
